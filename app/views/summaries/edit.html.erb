<h1>Edit Summary</h1>

<div class="row">
	<div class="col-md-4">
		<p>Guidelines for summaries:</p>
		<ul>
			<li>A summary is short and to the point</li>
			<li>Only one per post</li>
			<li>Keep it nice and tight</li>
		</ul>
	</div>
	<div class="col-md-8">
		<%= form_for @summary do |f| %>
		<div class="form-group">
			<%= f.label :name %>
			<%= f.text_field :name, class: 'form-control', placeholder: "Enter your summary name here" %>
		</div>
		<div class="form-group">
			<%= f.label :description %>
			<%= f.text_area :description, rows: 8, class:'form-control', placeholder: "Enter your description here" %>
		</div>
		<div class="form-group">
			<%= f.label :public, class: 'checkbox' do %>
			<%= f.check_box :public %> Public summary
			<% end %>
		</div>
		<% end %>
	</div>
</div>